<UserControl x:Class="InteractiveWorldMap.Views.ClusterMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True"
             Cursor="Hand">
    <UserControl.Resources>
        <!-- Hover animation -->
        <Storyboard x:Key="HoverInStoryboard">
            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                           Storyboard.TargetProperty="ScaleX"
                           To="1.15" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                           Storyboard.TargetProperty="ScaleY"
                           To="1.15" Duration="0:0:0.15"/>
        </Storyboard>
        <Storyboard x:Key="HoverOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                           Storyboard.TargetProperty="ScaleX"
                           To="1.0" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                           Storyboard.TargetProperty="ScaleY"
                           To="1.0" Duration="0:0:0.15"/>
        </Storyboard>
        
        <!-- Click animation -->
        <Storyboard x:Key="ClickStoryboard">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ScaleTransform"
                                          Storyboard.TargetProperty="ScaleX">
                <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="0.9"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="1.0"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ScaleTransform"
                                          Storyboard.TargetProperty="ScaleY">
                <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="0.9"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="1.0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RenderTransform>
            <ScaleTransform x:Name="ScaleTransform" 
                          ScaleX="1" ScaleY="1"
                          CenterX="20" CenterY="20"/>
        </Grid.RenderTransform>
        
        <!-- Stamp image or fallback shape -->
        <Grid x:Name="MarkerBodyHost">
            <Image x:Name="StampImage"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Stretch="Uniform"/>
        </Grid>
        
        <!-- Count badge background -->
        <Ellipse x:Name="BadgeEllipse"
                 Fill="White"
                 RenderOptions.EdgeMode="Aliased"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"/>
        
        <!-- Count text -->
        <TextBlock x:Name="CountText"
                   Text="0"
                   FontWeight="Bold"
                   Foreground="#D32F2F"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextAlignment="Center"/>
    </Grid>
</UserControl>
