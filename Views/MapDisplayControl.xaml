<UserControl x:Class="InteractiveWorldMap.Views.MapDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="Black">
    <Grid>
        <!-- Map Image - displays viewport crop -->
        <Image x:Name="MapImage" 
               Stretch="Fill" 
               HorizontalAlignment="Stretch" 
               VerticalAlignment="Stretch"
               RenderOptions.BitmapScalingMode="Fant"
               SnapsToDevicePixels="True"/>
        
        <!-- Marker Canvas - fills entire control, markers positioned in screen space -->
        <Canvas x:Name="MarkerCanvas"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="Transparent"/>

        <!-- Only the circular child targets participate in input; this canvas has no background. -->
        <Canvas x:Name="MarkerInteractionCanvas"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
